#include <iostream>
#include <vector>
#include <algorithm>
#include <string>

using namespace std;
typedef long long ll;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    ll t, a, b;
    cin >> t;

    for (ll i = 0; i < t; i++)
    {
        cin >> a >> b;
        vector<int> vec(a, 0);
        vector<int> vec2(10001, 0);
        for (int i = 0; i < a; i++)
        {
                }
    }
}